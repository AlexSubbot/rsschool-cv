## Aleksandr Subbotin

## +375 12 345 67 89 

Ten years of experience in technical support. I want to change my profession to a JS developer.

* Linux
* Networks 
* HTML, CSS, JS

        const polindromCheck = (myString) => {
        const normalizeString = myString.toLowerCase().replaceAll(' ', '');
        for (let i = 0; i < normalizeString.length; i++) {
            if (normalizeString[i] !== normalizeString[normalizeString.length - 1 - i]) {
            return false;
            }
        }
        return true;
        }

Catch bugs in a logging project.

## RGUITP

## B2